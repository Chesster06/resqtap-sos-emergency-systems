<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="@color/white"
    app:cardCornerRadius="22dp"
    app:cardElevation="0dp"
    app:strokeColor="#E2E8F0"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingTop="28dp"
        android:paddingEnd="24dp"
        android:paddingBottom="24dp">

        <!-- Title -->
        <TextView
            android:id="@+id/tv_tos_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@string/about_terms_header"
            android:textColor="#0F172A"
            android:textSize="17sp"
            android:textStyle="bold" />

        <!-- Scrollable Content Container -->
        <androidx.core.widget.NestedScrollView
            android:id="@+id/scroll_tos"
            android:layout_width="match_parent"
            android:layout_height="280dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="24dp"
            android:fadeScrollbars="false"
            android:fillViewport="true"
            android:scrollbarSize="4dp"
            android:scrollbarThumbVertical="@color/edit_profile_row_chevron"
            android:scrollbars="vertical">

            <TextView
                android:id="@+id/tv_tos_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:lineSpacingMultiplier="1.25"
                android:paddingEnd="10dp"
                android:text="@string/terms_and_conditions_full"
                android:textColor="#475569"
                android:textSize="13sp" />
        </androidx.core.widget.NestedScrollView>

        <!-- Accept Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_accept_tos"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:enabled="false"
            android:text="ACCEPT"
            android:textAllCaps="true"
            android:textColor="#94A3B8"
            android:textSize="14sp"
            android:textStyle="bold"
            app:backgroundTint="#E2E8F0"
            app:cornerRadius="12dp"
            app:elevation="0dp"
            app:rippleColor="#33000000" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
